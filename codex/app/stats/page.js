import { StatsPageShell } from "@/components/stats-page-shell";

export const metadata = {
  title: "Stats | KinKeeper",
  description: "Friendship analytics and interaction breakdown for KinKeeper.",
};

export default function StatsPage() {
  return <StatsPageShell />;
}
